<!DOCTYPE html>
<html>
	<head>
		<title>Comments - Love-for-the-Uglies</title>
		<link rel="stylesheet" href="/style/main.css">
	</head>
	<body>
		<%- include("partials/navbar") %>
		<main>
			<h1>Comments</h1>
			<p>Leave nice comments about your favourite ugly animal.</p>
			<ol id="comments-box">
				<% comments.forEach(comment => { %>
					<li class="comment">
						<a class="author" href="/profile/<%= comment.author %>"><%= comment.author %></a>
						<% if (user && (user.is_admin || user.username == comment.author)) { %><a href="/deleteComment/<%= comment._id %>"0>(remove comment)</a><% } %>
						<br>
						<span><%= comment.text %></span>
					</li>
				<% }) %>
			</ol>
			<% if (user != null) { %>
				<form id="comment-form" action="/postComment" method="post">
					<input type="text" name="comment" id="comment" placeholder="Comment" autofocus>
					<input type="submit" value="Send">
				</form>
			<% } %>
		</main>
	</body>
</html>